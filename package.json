{
  "name": "prodgate",
  "version": "1.0.0",
  "description": "Block destructive Terraform and OpenTofu changes in CI before they ship. Reads the plan, no cloud credentials.",
  "license": "MIT",
  "main": "dist/cli.js",
  "bin": {
    "prodgate": "dist/cli.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/prodgate-dev/prodgate.git"
  },
  "homepage": "https://github.com/prodgate-dev/prodgate",
  "bugs": {
    "url": "https://github.com/prodgate-dev/prodgate/issues"
  },
  "files": [
    "dist/"
  ],
  "scripts": {
    "prebuild": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\"",
    "build": "tsc",
    "build:action": "ncc build src/cli.ts -o action --no-source-map-register && node scripts/normalize-action-bundle.js",
    "typecheck": "tsc -p tsconfig.check.json",
    "prepare": "npm run build",
    "prepublishOnly": "npm run build",
    "dev": "ts-node src/cli.ts",
    "pretest": "npm run build",
    "test": "ts-node test/case_plan.ts && ts-node test/cli_test.ts && ts-node test/fuzz_test.ts"
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "keywords": [
    "terraform",
    "opentofu",
    "iac",
    "devsecops",
    "ci",
    "terraform-plan",
    "security"
  ],
  "engines": {
    "node": ">=20"
  },
  "author": "Prodgate Contributors",
  "dependencies": {
    "commander": "^14.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "@vercel/ncc": "^0.44.1",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.3"
  }
}
